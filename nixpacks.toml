[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["NODE_ENV=production pnpm run build"]

[start]
cmd = "sh -c 'NODE_ENV=production pnpm exec vite preview --host 0.0.0.0 --port ${PORT:-3000}'"
